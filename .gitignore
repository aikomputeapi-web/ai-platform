# ── Secrets (NEVER commit these) ──
.env
*.env
.env.*
!.env.unified.example

# ── Data volumes ──
data/
postgres_data/
redis_data/
backups/

# ── OS / Editor ──
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ── Logs ──
*.log
logs/

# ── Node ──
node_modules/

# ── Docker ──
.docker/

# ── Deploy state ──
.last_deployed_commit
.env.bak.*
*.local

# ── Temp files ──
nginx/nginx.conf.tmp

# ── Debug / scratch files (never commit these) ──
debug-*.js
debug-*.mjs
debug-*.ts
/a
kiro-cli.deb

# ── Standalone nested repos (managed via scripts/setup-omniroute.sh) ──
# OmniRoute and scratch are independent git repos cloned into the platform,
# NOT submodules. Keep them out of the parent repo's index.
/OmniRoute/
/scratch/
deploy-state/active-slot
